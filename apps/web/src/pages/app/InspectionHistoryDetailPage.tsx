// The history view and the active workspace share the same inspection detail
// experience in Phase 2; the history entry route renders it directly.
import InspectionDetailPage from "./InspectionDetailPage";

export default function InspectionHistoryDetailPage() {
  return <InspectionDetailPage />;
}
