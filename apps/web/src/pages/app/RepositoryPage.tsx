// The Product Repository is the products listing with search — same view as /app/products.
import ProductsPage from "./ProductsPage";

export default function RepositoryPage() {
  return <ProductsPage />;
}
