services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: metrascan
      POSTGRES_PASSWORD: metrascan
      POSTGRES_DB: metrascan
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
